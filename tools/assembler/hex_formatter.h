#include "buffer.h"
#include <string>

#ifndef _HEX_FORMATTER_H_
#define _HEX_FORMATTER_H_
buffer format_buffer(buffer);
unsigned int hexstr_to_int (std::string h);
std::string checksum (std::string);
#endif
